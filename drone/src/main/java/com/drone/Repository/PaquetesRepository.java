package com.drone.Repository;

import com.drone.Models.Paquetes;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PaquetesRepository extends JpaRepository<Paquetes,Long> {
}
